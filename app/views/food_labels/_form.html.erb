<%= form_for(@food_label) do |f| %>
  <% if @food_label.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_label.errors.count, "error") %> prohibited this food_label from being saved:</h2>

      <ul>
      <% @food_label.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
